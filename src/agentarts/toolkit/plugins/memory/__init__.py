"""AgentArts memory plugins package.

Sub-packages: ``ai_agent`` (platform-specific plugin assets), ``server``
(local adapter server), ``resources`` (docs/manifests + shared hook
scripts), ``installer`` (unified installer CLI).
"""
